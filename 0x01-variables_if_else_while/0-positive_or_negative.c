#include <stdio.h>
#include <math.h>

int main ();
 {
    int n ;
    printf("Enter number for n : ");
  if (n >= 0);
      if (n > 0);
      printf("positive");
      else,
      printf("is zero");
  else;
  printf("is negative");
    
 }
